# Provenance

This skill is synchronized from Threa's canonical `.agents/skills/threa-cli/SKILL.md` at commit `820fddf57901ea486712faccd3061b7a3553a7a9`.

Slopestyle changes the skill name and trigger description so it works as the global `threa` skill, removes em dashes without changing the command contract, and adds a Slopestyle-owned external-effect gate that every synchronization must preserve. Keep behavior synchronized with the product CLI.
